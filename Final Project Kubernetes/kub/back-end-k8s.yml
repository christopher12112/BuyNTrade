---
apiVersion: v1
kind: Service
metadata:
  name: back-end
  labels:
    app: back-end
spec:
  type: LoadBalancer
  selector:
    app: back-end
  ports:
    - name: http
      protocol: TCP
      port: 5001
      targetPort: 5001
      nodePort: 32001
---
